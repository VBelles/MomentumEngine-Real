Eventos:
	onGameStart()
	onPowerLevelChange(powerLevel)
	onEnemyKilled(enemyName)
	onPlayerKilled()
	onLevelStart_levelId()
	onTriggerEnter_triggerName(entityName)
	onTriggerExit_triggerName(entityName)
	onAltarDestroyed_altarName()
	onMechanismSystemActivated_mechanismSystemName()
	onMechanismSystemDeactivated_mechanisSystemmName()

Funciones:
	void callDelayed(float delay, string funcion, string params)

	void wait(float duration)
	void waitCondition(function func, string params)

	void blendInCamera(string cameraName, float timeToMixIn, CAMERA_PRIORITY, INTERPOLATOR)
	void copyRotationFromCamToCam(std::string copyCamName, std::string pasteCamName)
	
	float getPlayerHp()
	float setPlayerHp(float hp)
	void teleportPlayer(float x, float y, float z)
	void movePlayer(float x, float y, float z)
	void setPower(float power), cantidad de poder
	void setPlayerRespawnPosition(float x, float y, float z)
	bool takePlayerControl()
	bool givePlayerControl()
	void lockPlayerCameraInput()
	void unlockPlayerCameraInput()
	void disablePlayerOutline()
	void enablePlayerOutline()
	bool isPlayerGrounded()
	void stopPlayerVelocity()
	void changePlayerState(std::string state)
	void changePlayerConcurrentState(std::string state)
	void rotatePlayerTowards(float x, float z, float speed)
	void walkTo(float x, float z)

	string spawnEntityAt(PREFAB, float x, float y, float z)
	string spawnEntity(PREFAB)
	string spawnGolemAt(float x, float y, float z)
	string spawnGolem()
	string spawnBallAt(float x, float y, float z)
	string spawnBall()
	string spawnMedusaAt(float x, float y, float z)
	string spawnMedusa()
	void stopEntities()
	void resumeEntities()

	bool openDoor(string doorName, float time)
	bool closeDoor(string doorName, float time)

	void startSoundEvent(SOUND_EVENT)
	void stopSoundEvent(SOUND_EVENT, SOUND_STOP)
	void emitSoundEvent(SOUND_EVENT)

	int launchParticleAtPos(PARTICLE, float x, float y, float z)
	int launchParticleAtEntity(PARTICLE, string entityName)
	int launchParticleAtEntityOffset(PARTICLE, string entityName, float x, float y, float z)
	int launchParticleAtBone(PARTICLE, string entityName, string bone)
	int launchParticleAtBoneOffset(PARTICLE, string&entityName, string bone, float x, float y, float z)
	void killParticle(int particle_handle, float fade_out)

Constantes:
	PLAYER_NAME
	PLAYER_CAMERA
	GAME_CAMERA

	PREFAB_GOLEM
	PREFAB_BALL
	PREFAB_MEDUSA
	PREFAB_CHRYSALIS
	PREFAB_DREIDEL
	PREFAB_KIPPAH
	PREFAB_KIPPAH_DUMMY
	PREFAB_MURO
	PREFAB_PLATAFORMA_HEX 
	PREFAB_PLATAFORMA_RECT
	PREFAB_PLATAFORMA_ROCA
	PREFAB_PRISMA

	INTERPOLATOR_LINEAR
	INTERPOLATOR_QUAD_IN
	INTERPOLATOR_QUAD_OUT
	INTERPOLATOR_QUAD_IN_OUT
	INTERPOLATOR_CUBIC_IN
	INTERPOLATOR_CUBIC_OUT
	INTERPOLATOR_CUBIC_IN_OUT
	INTERPOLATOR_QUART_IN
	INTERPOLATOR_QUART_OUT
	INTERPOLATOR_QUART_IN_OUT
	INTERPOLATOR_QUINT_IN
	INTERPOLATOR_QUINT_OUT
	INTERPOLATOR_QUINT_IN_OUT
	INTERPOLATOR_BACK_IN
	INTERPOLATOR_BACK_OUT
	INTERPOLATOR_BACK_IN_OUT
	INTERPOLATOR_ELASTIC_IN
	INTERPOLATOR_ELASTIC_OUT
	INTERPOLATOR_ELASTIC_IN_OUT
	INTERPOLATOR_BOUNCE_IN
	INTERPOLATOR_BOUNCE_OUT
	INTERPOLATOR_BOUNCE_IN_OUT
	INTERPOLATOR_CIRCULAR_IN
	INTERPOLATOR_CIRCULAR_OUT
	INTERPOLATOR_CIRCULAR_IN_OUT
	INTERPOLATOR_EXPO_IN
	INTERPOLATOR_EXPO_OUT
	INTERPOLATOR_EXPO_IN_OUT
	INTERPOLATOR_SINE_IN
	INTERPOLATOR_SINE_OUT
	INTERPOLATOR_SINE_IN_OUT

	CAMERA_PRIORITY_DEFAULT
	CAMERA_PRIORITY_GAMEPLAY
	CAMERA_PRIORITY_TEMPORARY
	CAMERA_PRIORITY_DEBUG

	SOUND_EVENT_TEST
	SOUND_STOP_ALLOWFADEOUT
	SOUND_STOP_IMMEDIATE

	PARTICLE_ANIMATED_FIRE
	PARTICLE_SNOW
	PARTICLE_SPARKS
	PARTICLE_WILDFIRE